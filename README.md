# R Workflow for Validation of Analytical Data

# Author: Helge Wimmer

This is a small example ...

+ `data/` contains raw an processed data
+ `outputs/`contains content generated by this project such as summaries, plots and reports
+ `R/` contains function utilities used to process, analyze and visualize the data
+ `reports/` contains Quarto files for generating computational documents
+ `scripts/`contains the main workflow files of the project

To process this project, source `scripts/valid.R`